<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.desatek.lvsci">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Actividad_Principal"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Doctores"
            android:label="@string/title_activity_doctores"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FormRegistro" />
        <activity android:name=".iniciar_sesion" />
        <activity android:name=".Cuestionario" />
        <activity android:name=".Listado_Preguntas" />
        <activity
            android:name=".Agendar_Cita"
            android:label="@string/title_activity_agendar__cita"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Pacientes"
            android:label="@string/title_activity_pacientes"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>